# from dotenv import load_dotenv
# import os
# import pymongo
# load_dotenv()

# url = os.environ.get(
#     'MONGO_URI',
# )

# try:
#     client = pymongo.MongoClient(url)


# except Exception:
#     print('Error:' + Exception)

# db = client['authDAS']
# collection = db['users']
